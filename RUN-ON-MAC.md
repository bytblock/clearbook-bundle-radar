# Bundle Radar — run on this Mac

**Path:** `/Users/rooutt/clearbook/bundle-radar`

```bash
cd /Users/rooutt/clearbook/bundle-radar
npm run start:aggregator   # http://127.0.0.1:8787
npm run dev:dashboard      # http://127.0.0.1:5173
```

Verified 2026-09-20: `npm install` + `npm run build` OK; `/health` PAPER; dashboard HTTP 200.
LIVE stays off. MetaMask connect is in the dashboard UI.
